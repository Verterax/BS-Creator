using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using BranchingStoryCreator;
using System.Windows.Forms;

namespace TestBSC
{
    [TestClass]
    public class StoryTreeManagerTests
    {

        [TestMethod]
        public void CreateNewNode()
        {
            //StoryTree tree = new StoryTree();
           // TreeNode node = StoryTreeManager.GetNewNode(tree);

            // Assert
            //Assert.AreNotEqual(null, node);
        }
    }
}
